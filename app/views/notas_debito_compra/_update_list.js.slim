| $("#list").html("#{escape_javascript(render partial: 'list', format: 'html')}");
| $('#add-modal').modal('hide');
| $('#paginator').html("#{escape_javascript(paginate(@notas_debito_compra, remote: true)) }");
| $('#notas-debito-compra-size').text('#{@notas_debito_compra_size}');
