.row.row-page-title
  h2 Proveedores
  = link_to 'Agregar Proveedor', new_proveedor_path, class: 'btn btn-primary', remote: true
  //.btn-group.pull-right.print-link
  	//button.btn.btn-default.dropdown-toggle [type="button" data-toggle="dropdown"]
  	  //i.icon-print
  	  //| #{" Imprimir Listado "}
  	  //span.caret
  	//ul.dropdown-menu
  	  //li
  	    //= link_to 'Listado Actual', '#', class: 'listado-actual'
  	  //li

.row.form-search.row-grid
  = render 'search'

.row.search-message.row-grid
  h4.text-info.pull-left
    |Se han encontrado #{' '}
    span#proveedores-size #{@proveedores_size}
    |  registros
  = link_to '<i class="icon-print"></i> Imprimir'.html_safe, imprimir_todos_proveedores_path(format: 'pdf'), class: 'pull-right btn btn-default print-link', id: 'imprimir-link', target: '_blank', data: {url: imprimir_todos_proveedores_path(format: 'pdf')}

#list
  = render 'list'

#paginator
  = paginate @proveedores, remote: true

#add-form.col-md-4.hidden-print