<%= form_for(@componente_categoria) do |f| %>
  <% if @componente_categoria.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@componente_categoria.errors.count, "error") %> prohibited this componente_categoria from being saved:</h2>

      <ul>
      <% @componente_categoria.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
